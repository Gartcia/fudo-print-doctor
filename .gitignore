# Telemetria: la URL no se publica (repo publico)
telemetria.txt
telemetria.url
fudo-telemetria.txt

# Salidas y archivos temporales
resultado.json
FudoPrintDoctor-*.json
*.log
*.exe
*.zip

# Editores / SO
.vscode/
.idea/
Thumbs.db
desktop.ini
*~

# Contexto del proyecto para Claude Code: NO se publica.
# Menciona el fileId de la planilla de telemetria, pcIds de clientes y nombres de asesores.
contexto/

# Comandos locales de Claude Code: describen el flujo de auto-implementar y pushear a main,
# no es algo para publicar en un repo publico.
.claude/commands/
# Carpeta de distribucion al equipo: lleva el .cmd INTERNO con la URL de
# telemetria, el .msi de la App Nativa y utilidades de terceros (drivers,
# NetConfigTools). Nada de eso va al repositorio publico.
tools/FudoPrintDoctor/
FudoPrintDoctor-interno.cmd
# Instalador de la Nativa partido para pasarlo por TeamViewer (limite 25 MB).
# Son los mismos 60 MB del instalador, troceados: no van al repositorio publico.
tools/Nativa-partida/
tools/FudoPrintDoctor-NetConfigTools.zip
